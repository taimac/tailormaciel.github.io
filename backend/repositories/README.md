# Repositories (Infrastructure Layer)

Handles data access and integration with external services.

- Implements persistence logic (e.g., database CRUD).
- Follows the Repository pattern to abstract data sources.

**Learning:**  
Demonstrates dependency inversion and separation of business logic from infrastructure.
